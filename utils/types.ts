
export enum TransactionStatus {
    tsPending = "PENDING",
    tsConfirmed = "CONFIRMED",
    tsDeclined = "DECLINED"
}

export enum NodeType {
    ntFullNode = "FULL",
    ntLightNode = "LIGHT"
}

export enum LogType {
    ltError = "ERROR",
    ltInfo = "INFO"
}


